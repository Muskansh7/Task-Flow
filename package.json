{
  "name": "taskflow-root",
  "version": "1.0.0",
  "description": "Root manager for TaskFlow",
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../server/client-dist && mv dist/* ../server/client-dist/",
    "start": "cd server && npm install && node index.js"
  }
}