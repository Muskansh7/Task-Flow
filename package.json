{
  "name": "taskflow-root",
  "version": "1.0.0",
  "description": "Root manager for TaskFlow",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p server/client-dist && cp -r client/dist/* server/client-dist/",
    "start": "cd server && npm install && node index.js"
  }
}